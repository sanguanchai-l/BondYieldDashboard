version: '3.8'

services:
  bond-dashboard:
    build: .
    ports:
      - "8501:8501"
    environment:
      - FRED_API_KEY=${FRED_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
